# This directory contains tests for the azure_mcp_agent package.
# Note: This __init__.py is intentionally empty to avoid namespace conflicts
# with the actual azure_mcp_agent package in src/azure_mcp_agent/.
# When pytest runs, it will import azure_mcp_agent from src/ (via pythonpath in pytest.ini),
# not from this tests directory.
